services:
  name: swiftdeploy-app
  image: swift-deploy-1-node:latest
  port: 3000
  mode: stable
  version: '1.0'
  restart_policy: unless-stopped
  log_path: /var/log/app
nginx:
  image: nginx:latest
  port: 8080
  proxy_timeout: 30
network:
  name: swiftdeploy-net
  driver_type: bridge
